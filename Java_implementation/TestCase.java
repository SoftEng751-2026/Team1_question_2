package Java_implementation;
public class TestCase {
    int rows, cols, iterations, seed;
    TestCase(int rows, int cols, int iterations, int seed) {
        this.rows = rows;
        this.cols = cols;
        this.iterations = iterations;
        this.seed = seed;
    }
}